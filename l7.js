const myInfo = {
    name : 'samir',
    address : 'nuwakot',
    phoneNum : 3978438,
    age : 21
}

// 1: get output 'samir'
// console.log(myInfo.name)

// get the output 'Samir' lives in 'Nuwakot'        // Note : Check cases
// let capitalFName = myInfo.name.charAt(0).toUpperCase() + myInfo.name.slice(1);
// let capitalFAddress = myInfo.address.charAt(0).toUpperCase() + myInfo.address.slice(1);
// console.log(`${capitalFName} lives in ${capitalFAddress}`);

// Decrease the age by 30 and console the output
// myInfo.age -= 30;
// console.log(myInfo)


// console left side(AKA keys) of the object myInfo  
// let keys = Object.keys(myInfo);
// console.log(keys);

// console right side(AKA values) of the object myInfo
// let values = Object.values(myInfo);
// console.log(values);


// check if a property exists in an object
// console.log(Object.hasOwn(myInfo, 'name'));

